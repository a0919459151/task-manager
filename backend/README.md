

## 依賴
``` bash
npm init -y
npm install
npm install express mongoose @ts-rest/express @ts-rest/core zod
npm install -D typescript @types/express @types/node ts-node nodemon --legacy-peer-deps
npm install cors @types/cors --legacy-peer-deps
```